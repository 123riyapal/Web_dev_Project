<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>login</title>
    <style>
        body{
            background-color: cadetblue;
            display: flex;
            justify-content: center;
            align-items: center;
            max-height:100vh;
            max-width: 100vw;

        }
        button{
            background-color:paleturquoise;
            color: purple;
            padding: 12px 25px;
            font-size: 15px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            position: absolute;
            top: 50%;
            left:50%;
            transform: (-50%,-50%);
        }
        button:hover{
            box-shadow: 10px 10px 4px rgba(19, 75, 77, 0.453);
            transition-duration: 0ms;
        }
    </style>
</head>
<body>
    <h1>Hi, <%= name%> </h1>
    <form action="/logout" method="get">
        <button type="submit">log out</button>
    </form>
</body>
</html>